'use client'; //To use the component on client side
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button className='btn btn-primary' onClick={() => console.log("Clicked")}>Click here</button>
    </div>
  )
}

export default AddToCart